# =============================================================================
# Local Development Environment Configuration
# =============================================================================
# 
# CHOOSE YOUR DATABASE:
# 
# 1. SQLite (Recommended for quick start):
#    - No database installation required
#    - Perfect for testing and development
#    - Copy: cp local-env.sqlite.example local-env
#    - See: local-env.sqlite.example
#
# 2. PostgreSQL (Recommended for production-like setup):
#    - Same database as AWS production
#    - Requires PostgreSQL installation
#    - Copy: cp local-env.postgresql.example local-env
#    - See: local-env.postgresql.example
#
# =============================================================================
# QUICK START (SQLite):
# =============================================================================
# cp local-env.sqlite.example local-env
# # Edit local-env and add your OPENAI_API_KEY
# pip install -r requirements.txt
# python start.py --all
#
# =============================================================================
# PRODUCTION-LIKE SETUP (PostgreSQL):
# =============================================================================
# # Install PostgreSQL first (see docs/LOCAL-DEPLOYMENT-GUIDE.md)
# cp local-env.postgresql.example local-env
# # Edit local-env with your database credentials and API keys
# pip install -r requirements.txt
# alembic upgrade head
# python start.py --all
#
# =============================================================================

# This file is kept for backward compatibility
# Please use one of the specific example files above

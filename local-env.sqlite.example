# =============================================================================
# Local Development Environment Configuration - SQLite Version
# =============================================================================
# This configuration uses SQLite for quick testing and development
# No database installation required - perfect for getting started quickly!
#
# To use this configuration:
#   1. Copy this file: cp local-env.sqlite.example local-env
#   2. Update OPENAI_API_KEY with your actual key
#   3. Run: python start.py --all
#
# For production-like setup with PostgreSQL, see: local-env.postgresql.example
# =============================================================================

# Environment
ENVIRONMENT=local

# Deployment Path (where the application is deployed)
# Local: C:\crypto-ia (Windows) or /home/user/crypto-ia (Linux/macOS)
# AWS: /opt/crypto-ia
# Leave empty to use current directory
ENVIRONMENT_PATH=C:\crypto-ia

# Database (SQLite - No installation required!)
# The database file will be created automatically at: C:\crypto-ia\crypto_test.db
DATABASE_URL=sqlite:///./crypto_test.db
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10

# Web UI
WEB_UI_HOST=localhost
WEB_UI_PORT=10443
WEB_UI_PROTOCOL=http

# SSL Certificate (Optional for local development)
# SSL_CERT_PATH=./certs/local/cert.pem
# SSL_KEY_PATH=./certs/local/key.pem

# Data Collection
COLLECTION_START_DATE=2024-01-01
TOP_N_CRYPTOS=10  # Reduced for testing
COLLECTION_SCHEDULE=0 */6 * * *  # Every 6 hours
BINANCE_API_KEY=test_key
BINANCE_API_SECRET=test_secret

# Prediction Engine
MODEL_TYPE=LSTM  # or GRU
PREDICTION_HORIZON_HOURS=24
MODEL_RETRAIN_SCHEDULE=0 2 * * 0  # Weekly on Sunday at 2 AM
SEQUENCE_LENGTH=168  # 7 days of hourly data

# GenAI (REQUIRED - Get your key from https://platform.openai.com/api-keys)
OPENAI_API_KEY=your_openai_key_here
OPENAI_MODEL=gpt-4o-mini
OPENAI_MAX_TOKENS=500
OPENAI_TEMPERATURE=0.7

# Alert System (Disabled for testing)
ALERT_ENABLED=false
ALERT_THRESHOLD_PERCENT=10.0
ALERT_COOLDOWN_HOURS=4
SMS_PROVIDER=twilio
SMS_PHONE_NUMBER=+1234567890

# API
API_HOST=127.0.0.1
API_PORT=5000
API_KEY_REQUIRED=false  # Disabled for local dev
RATE_LIMIT_PER_MINUTE=100

# Streamlit
STREAMLIT_PORT=8501

# Security
SECRET_KEY=test_secret_key_for_development
ALLOWED_ORIGINS=http://localhost:10443

# Logging
LOG_LEVEL=INFO
LOG_FILE=logs/crypto_saas.log

# Log Retention
AUDIT_LOGS_RETENTION_DAYS=90
CHAT_HISTORY_RETENTION_DAYS=30
QUERY_AUDIT_LOGS_RETENTION_DAYS=365
SYSTEM_LOGS_RETENTION_DAYS=30
LOG_RETENTION_SCHEDULE=0 2 * * *  # Daily at 2 AM

# Development Options
DEBUG=true
FLASK_ENV=development
TESTING=false

# =============================================================================
# QUICK START GUIDE
# =============================================================================
# 1. Get OpenAI API Key: https://platform.openai.com/api-keys
# 2. Update OPENAI_API_KEY above
# 3. Install dependencies: pip install -r requirements.txt
# 4. Run: python start.py --all
# 5. Access:
#    - API: http://localhost:5000/api/health
#    - Dashboard: http://localhost:8501
#
# Note: SQLite is great for development but use PostgreSQL for production!
# =============================================================================
